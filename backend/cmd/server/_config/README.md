# App configuration directory

Environment configuration files hosted in this directory.

```
_config
|- base.yaml        # covers all configurations
|- development.yaml # overrides base.yaml content when the key presents on development environment
|- production.yaml  # overrides base.yaml content when the key presents on production environment
|- staging.yaml     # overrides base.yaml content when the key presents on staging environment
```
